{% extends '::layout.html.twig' %}
{% block title %}Nouveau texte{% endblock %}


{% block body %}
    <h3>Nouveau texte</h3>
    
    <div class="panel panel-default">
        <div class="panel-body">
            {{ form_start(form) }}
                <div class="form-group">
                    {{ form_row(form.texte) }}
                </div>
                <div class="form-group">
                    {{ form_row(form.selected) }}
                </div>
                <div class="form-group">
                    <input class="btn btn-default" type="submit" value="Enregistrer" />
                </div>
            {{ form_end(form) }}
        </div>
    </div>
    
    <a href="{{ path('content_index') }}" title="Retour">Retour à l'administration</a>

{% endblock %}
